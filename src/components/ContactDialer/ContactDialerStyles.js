import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  MainContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  },
  button: {
    width: "80%",
    padding: 6,
    backgroundColor: "#42add5",
    borderRadius: 7
  },

  TextStyle: {
    color: "#fff",
    fontSize: 18,
    textAlign: "center"
  }
});
